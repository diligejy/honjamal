name: smart-push
description: 테스트 검증 후 안전하게 푸시하는 워크플로우
steps:
  - run: npm test
  - if: last_exit_code != 0
    run: echo "테스트 실패! 자동 수정을 시도하거나 사용자에게 보고합니다."
  - run: git add . && git commit -m "auto-verified update" && git push origin master
